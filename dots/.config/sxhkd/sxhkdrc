# My SXHKD configuration.
# Written by Tiger Sachse.

# Quick-launch keymaps.
super + q
    rofi -show run
super + w
    google-chrome
super + e
    urxvt
super + a
    amazonchime
super + s
    thunderbird

# Close a node.
alt + shift + q
	bspc node -c

# Move between nodes and swap nodes around.
alt + {_,shift + } {h,j,k,l}
    bspc node --{focus,swap} {west,south,north,east}

# Resize nodes.
alt + shift + {a,s,w,d}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}
alt + {a,s,w,d}
	bspc node -z {right -20 0, top 0 20, bottom 0 -20, left 20 0}

# Switch between desktops.
alt + {1-3}
    bspc desktop -f '{1-3}'
alt + shift + {1-3}
    bspc node -d '{1-3}' --follow

# Toggle fullscreen.
alt + shift + f
	bspc node -t '~fullscreen'
